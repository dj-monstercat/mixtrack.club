test:
  override:
    - echo "placeholder circle.yml"
deployment:
  production:
    branch: master
    commands:
      - git remote add production $PRODUCTION_REMOTE_ADDRESS
      - git push production master
    
